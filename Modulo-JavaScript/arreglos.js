'use strict'
//como se declara un arreglo
const miArreglo = [1, 2, 3, 4, 5, 6, 7];
const otroArreglo = new Array('a', 'e', 'i', 'o', 'u');

console.log(miArreglo);

//Acceder a los arreglos

console.log(miArreglo[1]);

